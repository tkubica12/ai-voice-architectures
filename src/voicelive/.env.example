# Azure Voice Live API Configuration
# Copy this file to .env and fill in your values

# Required: Azure Voice Live endpoint
# Get this from your Microsoft Foundry resource in Azure Portal
AZURE_VOICELIVE_ENDPOINT=https://your-resource-name.services.ai.azure.com/

# Authentication (choose one method):

# Option 1: API Key authentication
AZURE_VOICELIVE_API_KEY=your-api-key-here

# Option 2: Azure AD / Managed Identity authentication
# Set USE_TOKEN_CREDENTIAL=true and ensure your identity has:
# - Cognitive Services User role
# - Azure AI User role
# USE_TOKEN_CREDENTIAL=true

# Optional: Default model (can be overridden via YAML config or CLI)
# Available models: gpt-4o, gpt-4o-mini, gpt-realtime, gpt-realtime-mini, gpt-4.1, gpt-5, phi4-mm-realtime
AZURE_VOICELIVE_MODEL=gpt-4o

# Optional: Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO
